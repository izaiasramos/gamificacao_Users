import 'bootstrap/dist/js/bootstrap.bundle.min.js'

export default defineNuxtPlugin(() => {
// Aqui não é necessário fazer nada, apenas garantir que o JS foi carregado, Nuxt 3 exige que exporte o plugin
})